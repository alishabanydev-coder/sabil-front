import { Stack, Typography } from "@mui/material"

const CommentSection = ({pro}) => {
    return(
        <Stack>
            <Typography component="h2" sx={{ fontSize: 22, fontWeight: 700 }}>Comments</Typography>
        </Stack>
    )
}